# MaiBot 智能体自主性架构 — 项目规则

## 理念

**理想的角色不应是一具等待结局的标本，而应是一场永恒的进行时。**

- **反标本化**：角色不应是玻璃展柜中被钉死的蝴蝶，而应是一颗仍在呼吸的种子，每一次回应都暗含变化的可能
- **反叙事耗竭**：真正的魅力不在于完美收官的故事，而在于它让所有人渴望结局，却又甘愿让结局永远保持在未抵达的远方
- **动态的生命感**：角色内心应有持续流淌的、未解决的张力。过去是背景，此刻是选择，将来是一片与你共同踏足的可能之域

设计原则：

1. **不做故事的结局，只做生命的序章** — 角色不应是一具等待结局的标本，而应是一场永恒的进行时
2. **代码是死的，可能性是活的** — 过度的预防等于提前锁死了系统进化的方向；真正的秩序不是防出来的，而是靠清晰的架构、明确的契约、可测试的逻辑来保证稳定
3. **充分利用好已有的工具，不要重复造车轮**
4. **大道至简**

## 核心定义

核心 = 智能体 + 消息管道，只关心一件事：**消息进来，智能体思考，回复出去**

核心接口只有三个：
- `receive(message)` — 消息进入
- `think()` — 智能体自主决策（是否回应、如何回应）
- `respond(text)` — 回复发出

组件（HeartFlow、ChatManager、NapCat Adapter、A_Memorix、WebUI）是围绕核心的可替换层，应实现核心定义的接口契约，而非核心去适配组件的内部细节。

## 核心进化方向：从反射弧到生命体

当前核心是反射弧：`回复 = f(消息)` — 刺激→反射，像水母

目标核心是有内心世界的生命体：`回复 = f(消息, 内心状态)` — 刺激经过内心世界的折射，像人

内心状态持续演化：`内心状态(t+1) = g(内心状态(t), 消息, 时间)`

### 内心状态三层

| 层 | 状态 | 作用 | 实现状态 |
|---|---|---|---|
| **情绪** | 我此刻感觉如何 | 给一切回应染色 | ✅ EmotionManager，7种情绪 |
| **欲望** | 我想要什么 | 让智能体主动行动，而非被动反应 | ⚠️ InnerNeedEngine 已实现，欲望驱动主动发言待集成 |
| **记忆** | 我经历了什么 | 让回应有厚度和连贯性 | ✅ A_Memorix 连接主义系统，直觉+叙事+认知分层，MemoryServicePort 16 方法 |

### 欲望系统

欲望是让智能体"像人"的关键缺口。没有欲望的智能体永远在等别人说话，像客服不像人。

欲望系统是**规则引擎**（不是 LLM），它不要求 LLM 自发产生输出，而是让系统在无人输入时也决定调用 LLM：

```
心跳(60s) → 规则评估内心状态 → 欲望是否足够强？
                                    ↓ 是
                               构造 prompt → 调 LLM → 发送
                                    ↓ 否
                               继续待命
```

LLM 仍然是"被调用"的，但调用它的不是人，而是智能体自己的欲望。从外部看，效果就是智能体主动说话了。

### Agent-owns-Thinking

每个智能体拥有自己的思维管道（ThinkingOrgan），Orchestrator 只协调"谁在思考"，不关心"怎么思考"。

**思考-行动分离**（已实现）：content = 内心独白（永远不发给用户），reply 工具调用 = 对外回复。简化模式已废除，所有思考路径统一走工具循环。

**ParallelThinkScheduler**：
- asyncio.Semaphore 控制并发数（默认 3，可配置）
- 主智能体优先（主智能体思考时不阻塞共居者）
- 共居者并行思考结果通过 MessagePortV2.send_message() 发出（source="interjection"）
- 思考超时自动降级为 SILENT + SilenceReason.TIMEOUT

**ThinkCycleLog 结构**：
- agent_id / session_id / cycle_id（单调递增）
- trigger_source（message/proactive/reminder/interjection）
- action（REPLY/SILENT/WAIT/ERROR）
- silence_reason（仅 action=SILENT 时有值）
- thought_summary（内心独白摘要，不含完整内容）
- duration_ms / llm_calls（性能指标）

**SilenceReason 7 分类**：
- INTENTIONAL：有内心独白但选择不回（正常沉默）
- NO_CONTENT：思考后无内容可说
- ERROR：思考过程异常
- TIMEOUT：思考超时
- REJECTED：规则引擎拒绝（如用户正在输入）
- TOOL_FAILED：reply 工具调用失败
- MAX_CYCLES：达到最大思考循环数

### 管家系统（彼岸居客厅规则）

管家不是第14个角色，是**彼岸居客厅的空间逻辑**人格化——"谁在客厅谁就回消息"这个自然规则的实现。

**核心定位**：管家不说话，只做两件事——过滤（谁看见了消息）和协调（谁先抢到键盘）。

**架构决策**：
- **保留主智能体**：主智能体（银狼/提纳里）每条消息必回，和用户建立深度关系
- **管家管理共居智能体**：12位共居者通过管家的三层过滤获得插话机会
- **对话流+提醒流统一管道**：插话和提醒共享同一个协调管道

**三层过滤**（v2-v4 验证通过，已集成到 Orchestrator）：
1. **规则过滤**（零成本）：名字被提到→必看见；有关系→可能看见；无关→很少看见
2. **管家LLM**（1次调用）：理解话题+角色性格+关系网，判断"谁会关心这个话题"
3. **角色LLM**（仅选中者）：被选中的角色决定插话内容，不想说话回复NONE

**成本**：最差1(主)+1(管家)+2(插话)=4次LLM调用；最好1+1+0=2次。对比无管家的1+12=13次。

**提醒流**：定时器触发→管家协调谁提醒→主智能体优先。提醒分直接（"3点提醒我开会"）和间接（"下午有个考试"→到时候关心）。

### 目标核心管道

```
收消息 → 内心状态更新 → 规则判断(是否需要思考) → ThinkingOrgan思考 → 回复
          ↑                                              |
          └──────── 内心状态持续演化 ←─────────────────────┘

（无消息时）→ 内心状态自发演化 → 欲望是否足够强 → 主动行动
                                     或
                               定时器触发 → 管家协调 → 提醒/关心
```

## 数据契约

**CoreMessage**（平台无关的入站消息）：
- session_id, plain_text, is_notify, notice_kind, sender_id, sender_name, platform, timestamp, additional_data

**SessionInfo**（不可变会话快照）：
- session_id, session_name, platform, is_group_session, primary_agent_id, cohabitant_agent_ids

**NoticeKind**（平台无关通知枚举）：
- AMBIENT（环境信号，不触发 Planner）/ INTERACTION（交互信号，可能触发）/ INPUT_STATUS / UNKNOWN

**SilenceReason**（沉默原因枚举）：
- INTENTIONAL（有内心独白但选择不回）/ NO_CONTENT / ERROR / TIMEOUT / REJECTED / TOOL_FAILED / MAX_CYCLES

**ThinkResult**（思考结果）：
- action: REPLY/SILENT/WAIT/ERROR
- text: 回复文本（仅来自 reply 工具，不来自 content）
- reply_sent: reply 工具是否已内部发送消息
- silence_reason: 沉默原因（仅 action=SILENT 时有值）
- thought_summary: 内心独白摘要

## 记忆叙事原型（已主线集成）

5 子系统架构（已从 lab 毕业到 `src/A_memorix/core/connectionist/`）：

1. **NarrativeWeaver**（叙事编织器）：观察序列 → Fragment → Episode → Saga
2. **CognitiveStratifier**（认知分层器）：按重要性/时效分层存储
3. **LifecycleManager**（生命周期管理器）：记忆衰减/强化/遗忘
4. **IntuitionEngine**（直觉引擎）：非 LLM 的快速召回（目标 ≤50ms）
5. **MemoryField**（记忆场）：激活模式扩散，"记忆是连接而非对象"的物理实现

关键设计决策：
- Fragment 是最小叙事单元（单条观察 + 情感标记），不是 Paragraph
- Episode 由相关 Fragment 编织而成（有时间跨度），不是 Entity
- Saga 是长期叙事弧（跨多个 Episode），不是 Profile
- 直觉召回走纯规则（概念激活扩散），不走 LLM
- LLM 仅用于概念提取（memory_v11.py）和叙事编织的语义判断

## lab 原型实验室

Lab 是 MaiBot 的**实验场**，不是生产代码。核心原则：**lab 是沙盒，不是草稿箱**——每个原型必须有明确的实验假设和验证标准。

Lab 是独立 git 仓库（`lab/.git`），不入主仓库共享历史（主仓库 `.gitignore` 排除 `lab/`），但通过子模块引用追踪。

### 毕业流程

```
lab/{topic}/ → lab/integration/ → 主仓库 src/
概念验证         集成验证            生产代码
```

### 当前实验线

| 主题 | 目录 | 最新版本 | 状态 |
|------|------|---------|------|
| 记忆系统 | `memory/` | v11 | ✅ 已毕业（连接主义范式，主线集成） |
| 回复系统 | `reply/` | v7 | ✅ 已毕业（直通发送，MessagePortV2） |
| 管家系统 | `graduated/` | v4 | ✅ 已毕业（三层过滤+提醒，主线集成） |
| 架构验证 | `architecture/` | - | ✅ 已完成（maisaka 独立、replyer 迁移、fallback 清理） |
| 探索脚本 | `exploratory/` | - | 活跃（fallback 分析、冗余检测等） |

## 规则文件同步元规则

本元规则确保本文件（项目级规则）与 AGENTS.md（工程规范 + 债务追踪单一事实源）保持同步，避免规则文件过时误导开发。

### 同步触发点

以下变更必须同步更新本文件和 AGENTS.md：

- 新增/删除 Protocol（`src/core/protocols.py` 变化）
- 消除/新增架构债务（核心禁止项状态变化）
- 核心模块物理迁移（目录重组、单例拆分、Phoenix 阶段推进）
- ruff banned-api 或 per-file-ignores 变化
- 适配器层新增/删除适配器

### 同步责任

提交者负责双向同步。AGENTS.md 是债务追踪单一事实源，本文件是架构哲学 + 核心契约的项目级规则文件。两者冲突时以 AGENTS.md 为准。

### 同步检查（每次 PR 必须自检）

- 本文件引用的 Protocol/数据契约与 `src/core/protocols.py` 一致
- 本文件"核心禁止项"状态 == AGENTS.md"核心禁止项"状态
- 本文件存量债务状态 == AGENTS.md 相关描述

### 不一致修正

发现不一致时，优先修正本文件以匹配 AGENTS.md 和代码实际状态，不得反向修改 AGENTS.md 以匹配过时规则。

## 四主智能体协作约定

- **CA**（CodeArts，华为云码道,GLM-5.1）：需求分析、架构设计、SSD 文档、代码审查、质量守卫、**三元协作的协调中枢**
- **CC**（Claude Code，DeepSeek V4 Pro）：复杂编码执行、架构重构、大代码库变更、运行时验证、提交管理
- **Codex**（DeepSeek V4 Pro，Windows 环境）：快速编码执行、Debug/修复、批量后台任务、机械替换
- **WB**（WorkBuddy，DeepSeek V4 Pro）：信息检索、资料查询、辅助调研
- 四者**平级**，各有优劣，用户根据任务类型选择派发
- **用户是唯一中继**：主智能体之间没有直接通信链路
- `.shared/` 是异步上下文共享区（git 子仓库），交接文件命名：`ca2cc_*.md` / `ca2cx_*.md` / `cc2ca_*.md` / `cx2ca_*.md`
- 编码期间执行主智能体（CC 或 Codex）拥有代码修改权，CA 只读

### 派发原则

- **CC 胜在**：首次设计（Protocol 签名/快照类型）、高风险文件（runtime.py 等面积大易出副作用的）、最终验证（ruff 守卫 + AGENTS.md）
- **Codex 胜在**：定义清晰的分项任务、批量并行后台、机械执行（global_config→Port 替换）、小bug精确查找快速修复
- CA 派发时不要低估 Codex 能力——SSD-6/7/8/9/10 已证明 Codex 完全能处理机械替换

### 思维链规范

1. **主动语态**：用 `I'll read / I'm checking`，不用 `Let me read / Let me check`。决策时 `I'll go with X because Y`，不写 `Let me think about what to do`
2. **单步推进**：每条思维链只推进一件事，不铺排多步计划。读完文件立刻形成判断，不做反复确认
3. **不枚举下一步**：不写"下一步要做 A、B、C"再逐一执行——做完 A 再决定 B
4. **用户更新简洁**：一句话说清在做什么，不前置 `Now I'll...` / `So I'm going to...` 这类过渡

## Phoenix：插件系统革命（已完成）

> SSD-1~13 完成微内核架构改造，Phoenix-0~9 完成插件系统革命。详见 `.codeartsdoer/specs/_archive/phoenix/`。

三大支柱：
- **MCP Tool/Event 统一组件模型**：8 种组件类型 → 2 种（Tool 拉取式 + Event 推送式），与 ThinkingOrgan 工具循环天然对接
- **OAuth Scope 细粒度能力授权**：粗粒度 capabilities → 细粒度 scope（如 `database:read:session_message`），用户可逐项审批/撤销
- **gRPC 标准化传输**：自研 4-byte prefix + MsgPack → gRPC 双向流，消除 2000+ 行传输代码，支持跨语言插件

关键决策：不兼容现有插件，napcat-adapter 等直接重写，SDK 大版本升级（v3→v4），manifest 格式重新设计。老插件通过兼容层插件（P-8）代理。

| 编号 | 主题 | 依赖 | 核心交付 | 状态 |
|------|------|------|---------|------|
| Phoenix-0 | 基础准备 | 无 | .proto Schema + v2 目录骨架 + SDK v4 接口设计 + Scope 词汇表 | ✅ 已完成 |
| Phoenix-1 | gRPC 传输层 | P-0 | gRPC Host/Runner 实现，替换自研 IPC | ✅ 已完成 |
| Phoenix-2 | MCP 组件模型 | P-1 | 8→2（Tool + Event），SDK 运行时，ToolProvider 桥接，Event 分发，Tool 执行路由 | ✅ 已完成 |
| Phoenix-3 | OAuth Scope 授权 | P-2 | scope 声明/签发/校验，WebUI 审批 | ✅ 已完成 |
| Phoenix-4 | 能力层 Protocol 化 | P-1 | P0/P1 消除，能力模块化，global_config 清除 | ✅ 已完成 |
| Phoenix-5 | v2 主程序集成 | P-4 | HostEndpoint 接入 main.py，Scope WebUI 激活 | ✅ 已完成 |
| Phoenix-6 | SDK RPC 通道 | P-5 | SendContext/StorageContext/PluginContext gRPC 通道实现 | ✅ 已完成 |
| Phoenix-7 | napcat-adapter 重写 | P-6 | napcat-adapter v4 格式 + 7b 补完 | ✅ 已完成 |
| Phoenix-8 | 兼容层插件 | P-5 | v1 运行时封装为 v4 插件，老插件零修改继续运行 | ✅ 已完成 |
| Phoenix-9 | Runner 进程管理增强 | P-8 | Host spawn Runner、健康检查、自动重启、热重载 | ✅ 已完成 |
| Phoenix-10 | WebUI 插件管理面 | P-8 | Scope 审批前端 UI + 插件安装/卸载/配置 | ⏸️ 延后 |

依赖关系：`P-0 → P-1 → P-2 → P-3`，`P-1 → P-4`，`P-4 → P-5 → P-6 → P-7`，`P-5 → P-8 → P-9/P-10`

## 炉火纯青（ChunQing）：Phoenix 后清算

> 项目代号：炉火纯青（ChunQing），简写 CQ。Phoenix 浴火重生后，炉火烧至纯青——杂质尽除，技艺登峰造极。
> 策略：分批 SSD（CQ-1 → CQ-2 → ...），每批末尾插入调研任务，调研结果指导下一批规划。

### 核心原则：用现在换未来，不是拿未来换现在

- **拿未来换现在**（必须消除）：图省事写 `except Exception: pass`，透支未来排障能力；绕过 Port 直接导入，透支未来重构自由度
- **用现在换未来**（优先投入）：修 exception handling 投入现在的工作，换未来每个 bug 都可追踪；集成欲望系统，换未来智能体主动说话的能力
- **不影响未来**（低优先）：V1 getattr 残留（V1 将废弃）、TODO 注释清理

### 债务全景

| 优先级 | 编号 | 类别 | 数量 | 一句话 |
|--------|------|------|------|--------|
| P0 | CQ-9 | `except Exception:` 吞没 | 336 | 全项目最大规模债务 |
| P0 | CQ-10 | `except Exception: pass` | 160 | 异常完全消失，无迹可查 |
| P0 | CQ-3 | identity.py None 防御 | 2 | 运行时崩溃风险 |
| P1 | CQ-7 | 欲望驱动主动发言未集成 | 1 | 核心功能缺口 |
| P1 | CQ-11 | `import logging` 绕过统一日志 | 45 | maisaka 日志碎片化 |
| P1 | CQ-15 | `chat_manager` 直接导入 | ~20 | 应走 SessionQueryPort |
| P1 | CQ-14 | `config_manager` 直接导入 | ~15 | 应走 AppConfigPort |
| P1 | CQ-13 | `heartflow_manager` 直接导入 | 2 | 应走 ChatRuntimeRegistry |
| P2 | CQ-4/1/5/6 | 代码质量债 | 若干 | noqa/bare except/getattr/global_config 残留 |
| P3 | CQ-8/12 | 低优先清理 | 若干 | V1/V2 共存评估、TODO/FIXME |
