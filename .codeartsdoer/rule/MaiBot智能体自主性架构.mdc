# MaiBot 智能体自主性架构 — 项目规则

## 理念

**理想的角色不应是一具等待结局的标本，而应是一场永恒的进行时。**

- **反标本化**：角色不应是玻璃展柜中被钉死的蝴蝶，而应是一颗仍在呼吸的种子，每一次回应都暗含变化的可能
- **反叙事耗竭**：真正的魅力不在于完美收官的故事，而在于它让所有人渴望结局，却又甘愿让结局永远保持在未抵达的远方
- **动态的生命感**：角色内心应有持续流淌的、未解决的张力。过去是背景，此刻是选择，将来是一片与你共同踏足的可能之域

设计原则：

1. **不做故事的结局，只做生命的序章** — 角色不应是一具等待结局的标本，而应是一场永恒的进行时
2. **代码是死的，可能性是活的** — 过度的预防等于提前锁死了系统进化的方向；真正的秩序不是防出来的，而是靠清晰的架构、明确的契约、可测试的逻辑来保证稳定
3. **充分利用好已有的工具，不要重复造车轮**
4. **大道至简**

## 运行环境

- Docker 环境，容器名 `maim-bot-core`
- 依赖管理用 uv，不用 pip
- Python 3.14.6
- 代码修改后需重启容器才能生效
- docker 环境外层有 maibot 和 sdk 的原码：
  - `E:\Users\lmq\MaiBot\MiMo-Code` — 智能体原码
  - `E:\Users\lmq\MaiBot\maibot-plugin-sdk` — SDK 原码
- 我和上游是两个不同的开发方向，运行环境差别也很大，更新 skill、拉取上游时需要仔细检查，需要往我的方向靠拢

## 双智能体协作

- **CodeArts**（华为云码道）：需求分析、架构设计、SSD 文档（spec/design/tasks）、代码审查、质量守卫
- **CC**（Claude Code）：编码执行、运行时验证、调试排障、提交管理
- **用户是唯一中继**：两个智能体之间没有直接通信链路，需要对方做事必须通过用户传达
- **共享区**：`.shared/`（git 子仓库），包含协作约定、活跃任务上下文、交接文件
- **交接流程**：CodeArts 写 `handoff/ca2cc_*.md` → 用户传达 → CC 读取执行 → CC 写 `handoff/cc2ca_*.md` → 用户传达 → CodeArts 审查
- **冲突避免**：编码期间 CC 拥有代码修改权，CodeArts 只读；紧急修改先通知用户

## 智能体自主性架构原则

1. **智能体决策权原则**：外部系统（bot.py、HeartFlow、ChatManager 等消息链路模块）不应替智能体做业务决策。消息是否需要回复、是否触发 Planner，应由智能体自身的规则引擎决定，而非在链路中硬编码过滤或分流。消息链路保持透明，智能体是消息的最终消费者和决策者。

2. **通知消息处理原则**：`is_notify=True` 的通知消息应到达智能体（通过 Orchestrator），由智能体自主分类处理：
   - 纯环境信号（如 input_status）→ 规则引擎判定不触发 Planner，仅调整生命力/环境上下文
   - 可能需要回应的通知（如 poke、入群）→ 规则引擎判定触发 Planner，智能体自主决定是否回复
   - 分类规则可配置，但决策权在智能体，不在链路层

3. **规则引擎优先原则**：待命状态的环境感知必须是纯规则计算，不调用 LLM。能用规则判断的决策（如"用户正在输入不需要回复"），不应交给 Planner 推理。规则调整参数而非替智能体决策——规则决定"是否触发 Planner"，Planner 决定"如何回应"。

4. **组件兼容核心原则**：核心定义接口契约，组件实现契约。核心不依赖组件的具体实现类，只依赖 Protocol。新增代码禁止引入对 chat_manager、send_service、HeartFlow 等组件具体实现的直接导入。

5. **记忆是连接而非对象原则**：记忆不是带标签的标本，而是概念之间的激活模式。分类学（Paragraph/Entity/Relation/Episode/Profile）肢解了活的经验——给标本贴更多标签（时间、情感、视角）无法修复被分类切断的联系。新记忆系统应以连接为第一公民：新记忆 = 新连接，遗忘 = 连接衰减，回忆 = 重新激活模式。智能体声明记忆性格（衰减率、情感敏感度、联想深度），记忆系统解读执行。

6. **Agent-owns-Thinking 原则**：每个智能体拥有独立 ThinkingOrgan 实例，Orchestrator 只协调"谁在思考"，不关心"怎么思考"。共居智能体可并行思考（ParallelThinkScheduler，asyncio.Semaphore 控制并发）。思维管道的状态、提示词、工具集由智能体自身持有，不被全局共享。

7. **思考-行动分离原则**：content = 内心独白（永不外发，用户看不到），reply 工具调用 = 对外回复。简化模式已废除，所有思考路径统一走工具循环。SilenceReason 区分"只想不回"（INTENTIONAL）和"异常沉默"（ERROR/TIMEOUT 等），ThinkCycleLog 结构化日志记录每次思考循环。

8. **拒绝脏数据原则**：与"不兜底"区分——"不兜底"指当值应该存在时直接使用，不用 `or ""`/`or None` 掩盖错误；"拒绝脏数据"指外部数据源返回 None 时不强行计算 fallback 写入数据库，而应跳过或报错。两者都是让错误完整暴露，但作用点不同：前者在消费端，后者在生产端。

9. **改主程序先请示原则**：插件在 `/plugins` 下独立仓库，改动主程序代码（src/）需先请求许可。本规则文件本身的修改也属主程序改动。配置文件只改模板 + 版本号，不改动 legacy_migration。提示词修改需三语同步（zh-CN / en-US / ja-JP）。

## 技术约束

- 心跳间隔 60 秒，13 个待命智能体单次评估 ~600ms
- 所有数值必须映射为自然语言描述，不暴露具体数值给 LLM
- 情绪状态系统是 MaiBot 核心功能，不是插件
- 提示词修改需三语同步（zh-CN / en-US / ja-JP）
- 配置文件修改只改模板，新增版本号，不改动 legacy_migration
- 会话 ID 规范：业务模块不应自行调用 `SessionUtils.calculate_session_id`
- A_Memorix 是 MaiBot 的核心记忆子系统，可自由修改，约束仅来自核心隔离和 Protocol 接口契约

## 代码风格

- 砍掉过度防御/兜底代码、不必要配置参数、冗余逻辑
- 不要总是想找兜底，一定要精准地找到问题的核心，兜底是不合适的、难以维护的
- 不要总是考虑 fallback，如果哪里有错误，一定要让它及时完整地暴露，而不是用 fallback 兜底掩盖过去
- 应该尽量减少使用 getattr 和 setattr，优先使用类属性直接访问

## 核心定义

核心 = 智能体 + 消息管道，只关心一件事：**消息进来，智能体思考，回复出去**

核心接口只有三个：
- `receive(message)` — 消息进入
- `think()` — 智能体自主决策（是否回应、如何回应）
- `respond(text)` — 回复发出

组件（HeartFlow、ChatManager、NapCat Adapter、A_Memorix、WebUI）是围绕核心的可替换层，应实现核心定义的接口契约，而非核心去适配组件的内部细节。

## 核心架构：微内核 + 接口契约

### 核心接口层

核心通过 Protocol 定义接口契约，组件实现这些契约。核心模块只依赖 Protocol，不依赖具体实现。

| 接口 | 职责 | 实现者 | 状态 |
|---|---|---|---|
| **MessagePortV2** | 统一消息发送（1 个方法 send_message） | SendServiceMessagePortV2 | ✅ 已实现（旧 MessagePort 7 方法已移除） |
| **SessionRepository** | 会话信息查询（不可变快照） | ChatManagerAdapter | ✅ 已实现 |
| **AgentRoutingService** | 智能体-会话路由 | ChatManagerRoutingAdapter | ✅ 已实现 |
| **ChatRuntime** | 运行时生命周期 | MaisakaHeartFlowChatting | ✅ 已实现 |
| **ChatRuntimeRegistry** | 运行时注册表（含 list_runtimes） | HeartflowRuntimeRegistry | ✅ 已实现 |
| **ChatRuntimeFactory** | 运行时工厂（打破 heartflow→maisaka 依赖） | MaisakaRuntimeFactory | ✅ 已实现 |
| **NoticeClassifier** | 平台无关通知分类 | NapCatNoticeClassifier | ✅ 已实现 |
| **MemoryServicePort** | 记忆服务（16方法：observe/recall/recall_with_intuition/derive_profile/reflect/weave_narrative/heartbeat_maintenance 等） | AMemorixMemoryServicePort | ✅ 已实现 |
| **SessionInfoPort** | 组件反向查询会话 | ChatManagerAdapter（通过注册点注入 A_memorix） | ✅ 已实现 |
| **SessionLifecyclePort** | 会话生命周期（创建/持久化/初始化） | ChatManagerAdapter | ✅ 已实现 |
| **SessionQueryPort** | 会话批量查询/路由元数据 | ChatManagerAdapter | ✅ 已实现 |
| **MessageRegistryPort** | 入站消息注册 | ChatManagerAdapter | ✅ 已实现 |
| **ThinkingOrgan** | 智能体思维管道 | ThinkingOrgan（具体类） | ✅ 已实现 |
| **ThinkingOrganFactory** | 思维管道工厂 | ThinkingOrganFactory（具体类） | ✅ 已实现 |

### 核心禁止项

以下模式是架构债务，新增代码禁止引入，存量代码逐步消除：

1. **禁止核心直接导入 chat_manager** — 25 个文件 54 处导入是最大病灶，核心模块只通过 SessionRepository / AgentRoutingService 接口查询
2. **禁止核心访问 chat_manager._agent_router** — 私有属性访问破坏封装，通过 AgentRoutingService 接口操作
3. **禁止核心持有 BotChatSession 可变引用** — SessionRepository 返回不可变 SessionInfo 快照
4. **禁止核心硬编码 napcat_* 字段** — 通知分类由平台适配器在入站时完成，核心只处理 NoticeKind 枚举
5. **禁止核心绕过 MessagePort 直接调用 send_service** ✅ 已消除 + ruff TID251 守卫 — 内置工具和插件运行时统一走 MessagePortV2，8 个旧函数 banned-api 封堵
6. **禁止核心导入 A_memorix 内部模块** ✅ 已消除（core/ 零违规导入 + ruff TID251 守卫 + CI AST 脚本） — 只通过 MemoryServicePort 或 host_service 公共 API 交互
7. **禁止 Orchestrator 通过 enqueue_proactive_task 模拟多智能体** — 插话通过管家协调直接触发目标智能体的 ThinkingOrgan

### 数据契约

**CoreMessage**（平台无关的入站消息）：
- session_id, plain_text, is_notify, notice_kind, sender_id, sender_name, platform, timestamp, additional_data

**SessionInfo**（不可变会话快照）：
- session_id, session_name, platform, is_group_session, primary_agent_id, cohabitant_agent_ids

**NoticeKind**（平台无关通知枚举）：
- AMBIENT（环境信号，不触发 Planner）/ INTERACTION（交互信号，可能触发）/ INPUT_STATUS / UNKNOWN

**SilenceReason**（沉默原因枚举）：
- INTENTIONAL（有内心独白但选择不回）/ NO_CONTENT / ERROR / TIMEOUT / REJECTED / TOOL_FAILED / MAX_CYCLES

**ThinkResult**（思考结果）：
- action: REPLY/SILENT/WAIT/ERROR
- text: 回复文本（仅来自 reply 工具，不来自 content）
- reply_sent: reply 工具是否已内部发送消息
- silence_reason: 沉默原因（仅 action=SILENT 时有值）
- thought_summary: 内心独白摘要

## 核心进化方向：从反射弧到生命体

当前核心是反射弧：`回复 = f(消息)` — 刺激→反射，像水母

目标核心是有内心世界的生命体：`回复 = f(消息, 内心状态)` — 刺激经过内心世界的折射，像人

内心状态持续演化：`内心状态(t+1) = g(内心状态(t), 消息, 时间)`

### 内心状态三层

| 层 | 状态 | 作用 | 实现状态 |
|---|---|---|---|
| **情绪** | 我此刻感觉如何 | 给一切回应染色 | ✅ EmotionManager，7种情绪 |
| **欲望** | 我想要什么 | 让智能体主动行动，而非被动反应 | ⚠️ InnerNeedEngine 已实现，欲望驱动主动发言待集成 |
| **记忆** | 我经历了什么 | 让回应有厚度和连贯性 | ✅ A_Memorix 连接主义系统，直觉+叙事+认知分层，MemoryServicePort 16 方法 |

### 欲望系统

欲望是让智能体"像人"的关键缺口。没有欲望的智能体永远在等别人说话，像客服不像人。

欲望系统是**规则引擎**（不是 LLM），它不要求 LLM 自发产生输出，而是让系统在无人输入时也决定调用 LLM：

```
心跳(60s) → 规则评估内心状态 → 欲望是否足够强？
                                    ↓ 是
                               构造 prompt → 调 LLM → 发送
                                    ↓ 否
                               继续待命
```

LLM 仍然是"被调用"的，但调用它的不是人，而是智能体自己的欲望。从外部看，效果就是智能体主动说话了。

### Agent-owns-Thinking

每个智能体拥有自己的思维管道（ThinkingOrgan），Orchestrator 只协调"谁在思考"，不关心"怎么思考"。

**思考-行动分离**（已实现）：content = 内心独白（永远不发给用户），reply 工具调用 = 对外回复。简化模式已废除，所有思考路径统一走工具循环。

**当前**：
- 每个智能体持有自己的 ThinkingOrgan 实例
- Orchestrator 协调执行顺序，不替智能体做决策
- 管家在 Orchestrator 层协调，Planner 不知道多智能体的存在
- 支持并行思考（多个智能体同时思考不同话题）
- ThinkCycleLog 结构化日志，SilenceReason 区分只想不回和异常沉默

**ParallelThinkScheduler**：
- asyncio.Semaphore 控制并发数（默认 3，可配置）
- 主智能体优先（主智能体思考时不阻塞共居者）
- 共居者并行思考结果通过 MessagePortV2.send_message() 发出（source="interjection"）
- 思考超时自动降级为 SILENT + SilenceReason.TIMEOUT

**ThinkCycleLog 结构**：
- agent_id / session_id / cycle_id（单调递增）
- trigger_source（message/proactive/reminder/interjection）
- action（REPLY/SILENT/WAIT/ERROR）
- silence_reason（仅 action=SILENT 时有值）
- thought_summary（内心独白摘要，不含完整内容）
- duration_ms / llm_calls（性能指标）

**SilenceReason 7 分类**：
- INTENTIONAL：有内心独白但选择不回（正常沉默）
- NO_CONTENT：思考后无内容可说
- ERROR：思考过程异常
- TIMEOUT：思考超时
- REJECTED：规则引擎拒绝（如用户正在输入）
- TOOL_FAILED：reply 工具调用失败
- MAX_CYCLES：达到最大思考循环数

### 管家系统（彼岸居客厅规则）

管家不是第14个角色，是**彼岸居客厅的空间逻辑**人格化——"谁在客厅谁就回消息"这个自然规则的实现。

**核心定位**：管家不说话，只做两件事——过滤（谁看见了消息）和协调（谁先抢到键盘）。

**架构决策**：
- **保留主智能体**：主智能体（银狼/提纳里）每条消息必回，和用户建立深度关系
- **管家管理共居智能体**：12位共居者通过管家的三层过滤获得插话机会
- **对话流+提醒流统一管道**：插话和提醒共享同一个协调管道

**三层过滤**（v2-v4 验证通过，已集成到 Orchestrator）：
1. **规则过滤**（零成本）：名字被提到→必看见；有关系→可能看见；无关→很少看见
2. **管家LLM**（1次调用）：理解话题+角色性格+关系网，判断"谁会关心这个话题"
3. **角色LLM**（仅选中者）：被选中的角色决定插话内容，不想说话回复NONE

**成本**：最差1(主)+1(管家)+2(插话)=4次LLM调用；最好1+1+0=2次。对比无管家的1+12=13次。

**提醒流**：定时器触发→管家协调谁提醒→主智能体优先。提醒分直接（"3点提醒我开会"）和间接（"下午有个考试"→到时候关心）。

### 目标核心管道

```
收消息 → 内心状态更新 → 规则判断(是否需要思考) → ThinkingOrgan思考 → 回复
          ↑                                              |
          └──────── 内心状态持续演化 ←─────────────────────┘

（无消息时）→ 内心状态自发演化 → 欲望是否足够强 → 主动行动
                                     或
                               定时器触发 → 管家协调 → 提醒/关心
```

## 架构变革路线

### 已完成

- ✅ **MessagePort** — 核心→外部的发送接口契约（message_port.py）
- ✅ **管家系统集成** — Butler 三层过滤+提醒+插话（butler.py, reminder.py）
- ✅ **管家管道集成** — Orchestrator 中接入提醒创建、管家插话、提醒心跳
- ✅ **ChatLoopServiceAdapter** — ChatRuntime Protocol 依赖，核心不直接依赖 runtime
- ✅ **生命力管理** — VitalityManager + InnerNeedEngine + 心跳评估
- ✅ **环境感知** — AmbientAwarenessProcessor + StateAwareRuleEngine
- ✅ **Lab 原型验证** — 管家v1-v4、提醒v4、共居、欲望全部验证通过
- ✅ **核心 Protocol 层** — 13个Protocol（MessagePortV2/SessionRepository/AgentRoutingService/ChatRuntime/ChatRuntimeRegistry/NoticeClassifier/MemoryServicePort/SessionInfoPort/SessionLifecyclePort/SessionQueryPort/MessageRegistryPort/ThinkingOrgan/ThinkingOrganFactory）
- ✅ **核心数据模型** — NoticeKind/CoreMessage/SessionInfo/ThinkContext/ThinkAction/ThinkResult + CycleDetail迁移
- ✅ **适配器层** — 6个适配器（ChatManagerAdapter 5合1/ChatManagerRoutingAdapter/HeartflowRuntimeRegistry/NapCatNoticeClassifier/AMemorixMemoryServicePort/SendServiceMessagePortV2）
- ✅ **核心模块迁移** — Orchestrator/VitalityManager/ChatLoopAdapter/HeartflowManager 从chat_manager具体类迁移到Protocol接口
- ✅ **通知分类统一** — 消除AMBIENT_NOTICE_SUBTYPES重复定义和napcat_字段泄漏
- ✅ **Agent-owns-Thinking** — 每个智能体拥有独立ThinkingOrgan，think()/think_proactive()直接调用LLM
- ✅ **消灭enqueue_proactive_task hack** — 管家插话/提醒触发改为直接调用ThinkingOrgan + MessagePort.send()
- ✅ **并行思考** — ParallelThinkScheduler（asyncio.Semaphore控制并发）
- ✅ **A_memorix 隔离** — person_profile通过MemoryServicePort调用，SDKMemoryKernel完全隔离（core/ 零违规导入 28→0，AMemorixServicePorts 构造注入）
- ✅ **思考-行动分离** — content=内心独白（用户看不到），reply工具=对外回复；废除简化模式；SilenceReason/CycleStatus/ThinkCycleLog 结构化日志；Orchestrator 4个消费点 reply_sent 检查；Embodied Prompt 三语强化

### ChatManager 单例拆分（SSD-2，已完成）

ChatManager 从 604 行瘦身至 143 行，拆分为 6 个职责单一的子模块：

| 子模块 | 职责 |
|---|---|
| SessionStore | 会话内存缓存 + 持久化 |
| MessageRegistry | 入站消息注册 + 缓存 |
| SessionNameCache | 会话展示名称缓存 |
| SessionResolver | 平台/群/用户 → session_id 解析 |
| BindingRestorer | 智能体绑定恢复 |
| SessionLifecycle | 会话创建/获取/初始化 |

ChatManagerAdapter 通过 5 合 1 Protocol（SessionRepository/SessionInfoPort/SessionLifecyclePort/SessionQueryPort/MessageRegistryPort）包裹这 6 个子模块。当前仍通过 `_ensure_chat_manager()` 延迟拿单例，SSD-3 将改为依赖注入（见存量债务表）。

### 记忆叙事原型（已主线集成）

5 子系统架构（已从 lab 毕业到 `src/A_memorix/core/connectionist/`）：

1. **NarrativeWeaver**（叙事编织器）：观察序列 → Fragment → Episode → Saga
2. **CognitiveStratifier**（认知分层器）：按重要性/时效分层存储
3. **LifecycleManager**（生命周期管理器）：记忆衰减/强化/遗忘
4. **IntuitionEngine**（直觉引擎）：非 LLM 的快速召回（目标 ≤50ms）
5. **MemoryField**（记忆场）：激活模式扩散，"记忆是连接而非对象"的物理实现

关键设计决策：
- Fragment 是最小叙事单元（单条观察 + 情感标记），不是 Paragraph
- Episode 由相关 Fragment 编织而成（有时间跨度），不是 Entity
- Saga 是长期叙事弧（跨多个 Episode），不是 Profile
- 直觉召回走纯规则（概念激活扩散），不走 LLM
- LLM 仅用于概念提取（memory_v11.py）和叙事编织的语义判断

待实现：WebUI 记忆可视化（recall_with_intuition 便捷方法已就绪）。

### 待后续

- ✅ **MessagePort 全面采用** — v2 阶段 1-5 全部完成，旧 MessagePort Protocol（7 方法）已移除，统一为 MessagePortV2（1 个方法 send_message）
- ✅ **SDKMemoryKernel 完全隔离** — `src/A_memorix/core/` 零违规导入（28→0），所有外部依赖通过 AMemorixServicePorts 构造注入，ruff TID251 守卫 + CI AST 脚本
- ✅ **记忆系统范式迁移** — NEW_INDEPENDENT 阶段完成，分类学代码（graph_ops/）保留但零调用（已标记 DEPRECATED），所有请求走连接主义系统（`src/A_memorix/core/connectionist/`）；5 子系统（NarrativeWeaver/CognitiveStratifier/LifecycleManager/IntuitionEngine/MemoryField）已主线集成，通过 host_service API 暴露
- ✅ **记忆系统与核心架构差距（mem_core_gap）** — 6 批编码完成，21/29 项差距覆盖；MemoryServicePort 10→16 方法；异常子类体系；单例化；数据类型下放 common 层；A_memorix/core/ 零运行时 core 导入；直觉接入思考循环；情绪联动

### 记忆系统与核心架构差距（mem_core_gap，已完成）

**6 批编码全部完成**，21/29 项差距覆盖，8 项不在本期范围。

**关键变更**：
1. MemoryServicePort 从 10 方法扩展到 16 方法（新增 recall/recall_with_intuition/derive_profile/reflect/weave_narrative/heartbeat_maintenance，删除 ingest_text）
2. 异常子类体系：MemoryServiceError → TemporaryMemoryError / PermanentMemoryError / MemoryNotFoundError
3. 单例模式：`get_memory_service_port()` + `reset_memory_service_port()` 替代 13 处独立实例化
4. 数据类型下放 common 层：`src/common/memory_types.py`（8 个纯数据类型），`src/common/memory_utils.py`（从 core 迁移），core/types.py 重新导出
5. A_memorix/core/ 零运行时 core 导入：4 文件切到 common，1 个改 AMemorixServicePorts 注入
6. observe_experience() 统一 ObserveRequest 对象
7. MemoryWriteResult 扩展（新增 observation_id / concept_names）
8. 直觉接入思考循环：`_build_think_context()` 调用 `recall_with_intuition()`，填充 `memory_snippets` + `intuition_context`
9. heuristic_injector 直觉优先：`recall_with_intuition()` + `search()` 降级
10. 情绪联动：ExperienceWriter 新增 `emotion_manager` 注入，实时情绪→valence 自动推导

**未覆盖的 8 项差距**（不在本期范围）：
- G16 host_service 直接访问 kernel 私有属性（修复成本高，影响低）
- G18 Agent-owns-Thinking 与记忆性格未联动（agent_id 参数已传递，深度联动待后续）
- G19 管家系统与记忆系统未联动（需新增关系查询接口）
- G21 叙事弧未接入智能体认知（weave_narrative 已暴露，深度集成待后续）
- G22 AsyncWriteQueue 延迟启动竞态（已有保护机制）
- G23 ModelConfigPort 注入时序无检查（已有 None 保护）
- G24 记忆性格注册窗口期（需核心调度时序保证）
- G28 A_memorix 内部 322 处 bare except（修复成本极高，逐个审查需单独排期）

### 存量债务（已消除/剩余）

| 债务 | 原影响范围 | 原严重度 | 当前状态 |
|---|---|---|---|
| chat_manager 全局单例导入 | 25 个文件 54 处 | 极高 | ✅ 核心模块已通过Protocol隔离，剩余在适配器层（唯一允许的地方） |
| HeartFlow ↔ Maisaka 循环依赖 | 9+ 文件 | 高 | ✅ ChatRuntime Protocol + ChatRuntimeRegistry 打破类型层循环 |
| napcat_* 字段泄漏到核心 | 3 处 | 高 | ✅ NoticeClassifier + NoticeKind 枚举统一 |
| send_service 绕过 MessagePort | 6 个文件 12 处 | 中 | ✅ 已消除 + ruff TID251 守卫（8 个旧函数 banned-api 封堵） |
| A_memorix 内部函数泄漏 | 2 处 | 中 | ✅ person_profile 已隔离，SDKMemoryKernel 完全隔离（core/ 零违规导入） |
| chat_manager 单例物理退役 | 适配器层 | 中 | ⬜ 待 SSD-3 推进（ChatManagerAdapter 通过 `_ensure_chat_manager()` 延迟拿单例，Protocol 已隔离类型层，物理单例未拆） |
| maisaka → chat 物理依赖 | 19 处导入 | 中 | ⬜ 待架构革命推进（maisaka 单向依赖 chat 的 SessionMessage/CycleDetail/utils/heartflow_manager，类型层无循环，物理层靠延迟导入规避） |

## 规则文件同步元规则

本元规则确保本文件（项目级规则）与 AGENTS.md（工程规范 + 债务追踪单一事实源）保持同步，避免规则文件过时误导开发。

### 1. 同步触发点

以下变更必须同步更新本文件和 AGENTS.md：

- 新增/删除 Protocol（`src/core/protocols.py` 变化）
- 消除/新增架构债务（核心禁止项状态变化）
- 核心模块物理迁移（目录重组、单例拆分、SSD 阶段推进）
- ruff banned-api 或 per-file-ignores 变化
- 适配器层新增/删除适配器

### 2. 同步责任

提交者负责双向同步。AGENTS.md 是债务追踪单一事实源，本文件是架构哲学 + 核心契约的项目级规则文件。两者冲突时以 AGENTS.md 为准。

### 3. 同步检查（每次 PR 必须自检）

- 本文件"核心接口层"表格行数 == `src/core/protocols.py` 中 Protocol 数量
- 本文件"核心接口层"实现者列引用的类名在代码中存在（无已删除的类名）
- 本文件"核心禁止项"状态 == AGENTS.md"核心禁止项"状态
- 本文件"存量债务表"状态 == AGENTS.md 相关描述

### 4. 不一致修正

发现不一致时，优先修正本文件以匹配 AGENTS.md 和代码实际状态，不得反向修改 AGENTS.md 以匹配过时规则。

若 AGENTS.md 也过时，先修正 AGENTS.md（基于代码实际状态），再同步本文件。
