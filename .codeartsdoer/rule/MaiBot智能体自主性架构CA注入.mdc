# MaiBot 智能体自主性架构 — 项目规则

> 本文件：架构哲学 + 核心契约。硬性规则见 `AGENTS.md`，工作手册见 `CLAUDE.md`，债务追踪见 `.codeartsdoer/specs/memo/zg_cast_bone_research.md`。

## 理念

**理想的角色不应是一具等待结局的标本，而应是一场永恒的进行时。**

1. **不做故事的结局，只做生命的序章**
2. **人的本质是一切社会关系的总和**
3. **代码是死的，可能性是活的** — 真正的秩序靠清晰的架构、明确的契约、可测试的逻辑保证稳定
4. **充分利用好已有的工具，不要重复造车轮**
5. **大道至简**

## 核心定义

核心 = 智能体 + 消息管道，只关心一件事：**消息进来，智能体思考，回复出去**

核心接口只有三个：
- `receive(message)` — 消息进入
- `think()` — 智能体自主决策
- `respond(text)` — 回复发出

组件是围绕核心的可替换层，应实现核心定义的接口契约，而非核心去适配组件的内部细节。

## 核心进化方向：从反射弧到生命体

当前核心是反射弧：`回复 = f(消息)` — 刺激→反射，像水母

目标核心是有内心世界的生命体：`回复 = f(消息, 内心状态)` — 刺激经过内心世界的折射，像人

内心状态持续演化：`内心状态(t+1) = g(内心状态(t), 消息, 时间)`；无消息时内心状态自发演化，欲望够强则主动行动，或定时器触发管家协调。

### 内心状态三层

| 层 | 状态 | 作用 |
|---|---|---|
| **情绪** | 我此刻感觉如何 | 给一切回应染色 |
| **欲望** | 我想要什么 | 让智能体主动行动，而非被动反应 |
| **记忆** | 我经历了什么 | 让回应有厚度和连贯性 |

欲望系统是**规则引擎**（不是 LLM）。

### Agent-owns-Thinking

每个智能体拥有自己的思维管道（ThinkingOrgan），Orchestrator 只协调"谁在思考"，不关心"怎么思考"。思考-行动分离：content = 内心独白（永远不发给用户），reply 工具调用 = 对外回复。

### 管家系统（彼岸居客厅规则）

管家不是第 14 个角色，是**彼岸居客厅的空间逻辑**人格化——"谁在客厅谁就回消息"。管家不说话，只做两件事：过滤（谁看见了消息）和协调（谁先抢到键盘）。

## OS→MaiBot 映射

| OS 概念 | MaiBot 对应 |
|---------|------------|
| 内核 | 核心（Orchestrator + MessagePort） |
| 系统调用 | Protocol 接口 |
| 设备驱动 | 适配器（napcat-adapter 等） |
| 文件系统 | A_Memorix（记忆存储） |
| 进程 | 插件（gRPC Runner 隔离） |
| init/systemd | PluginRuntime（插件生命周期管理） |
| /proc | WebUI（系统内省） |
| 信号 | Hook |

## 数据契约

核心数据类型定义见 `src/core/protocols.py`，本文件只列契约名称：
- **CoreMessage** — 平台无关的入站消息
- **SessionInfo** — 不可变会话快照
- **NoticeKind** — 平台无关通知枚举
- **SilenceReason** — 沉默原因枚举
- **ThinkResult** — 思考结果

## 五主智能体协作约定

- **CA**：需求分析、架构设计、SSD 文档、代码审查、质量守卫、**三元协作的协调中枢（辅助）**
- **CC**：复杂编码执行、架构重构、大代码库变更、运行时验证、提交管理、**三元协作的协调中枢**（和 CA 协调）、逐步由dsh替代
- **Codex**：由 CC 通过插件直接调用
- **WB**：信息检索、资料查询、辅助调研
- **dsh**逐步代替CC和CX
- 五者**平级**，各有优劣，用户根据任务类型选择派发
- **用户是唯一中继**
- `.shared/` 是异步上下文共享区（git 子仓库），交接文件命名：`ca2cc_*.md` / `ca2cx_*.md` / `cc2ca_*.md` / `cx2ca_*.md`
- 编码期间执行主智能体（CC 或 Codex）拥有代码修改权，CA 只读
- **worktree 规则**：并行编码必须用独立 worktree 隔离（即用即建、合并即扔）；禁止在主仓库工作区切分支/合并
- **worktree 注意点**（均为实际踩坑记录）：
  - 主仓库工作区的未提交改动会跟随切分支流动——并行期间主仓库保持不动
  - Windows/WSL 两侧对同一 worktree 的路径视角不同（E:\ vs /mnt/e/），跨侧查看会显示 prunable——**勿跨侧 prune**，会删除对方侧的登记
  - bind mount 目录（如 lab/）在容器内 `rm -rf` 会直接删宿主文件——清理前确认目录是否挂载
  -在main干活需要许可，一般在工作树干活，不要擅自合并
  -不要评估工时
  -喜欢 git log --graph 的彩色分支拓扑线条

### 思维链规范

1. **主动语态**：用 `I'll read / I'm checking`，不用 `Let me read / Let me check`
2. **单步推进**：每条思维链只推进一件事，不铺排多步计划
3. **不枚举下一步**：做完 A 再决定 B
4. **用户更新简洁**：一句话说清在做什么

## 设计参考铁律（2026-08-17 用户拍板）

- **设计系统类**：必须标注 Linux 内核源码参考（importantClone/linuxclone/linux/ 对应机制文件）
- **设计智能体类**：必须标注 dsh 源码参考（importantClone/DEEPSEEKCLONE/deepseek-harness/ 对应包）
- **插件哲学**：MaiBot 插件体系重点从 dsh 学习（47 包 Cordis 插件架构是第一参考，其他框架仅补充）
- **dsh 参考文档地图**（做设计/调研先翻说明书再翻源码——全部在 DEEPSEEKCLONE/deepseek-harness/docs/ 下，有中文版）：architecture.md（总览）/ capability-seams.md（一切皆插件落地）/ config-catalog.md（132KB 配置目录——模型配置分层直接参考）/ tool-catalog.md + tool-execution-pipeline.md（工具）/ defensive-patterns.md（缺陷类防御规则——生命周期/并发代码前必读）/ event-producer-consumer.md（事件）/ agent-lifecycle.md（生命周期）/ .agents/skills/（团队 11 skill——工作流即插件）；详见 .shared/decisions/dsh_team_skills_observation_0817.md
- 理由：ZG 对标 Linux、模型特化对标 harness——两者是最重要的参考，不标注=设计无据

## SSD 启动必要条件

需求明确，行事有方向。启动 SSD 前必须能回答：做什么、为什么做、怎么验证做对了。
方案要解释清楚
## 工程规则（2026-08-20 迁移自 .codeartsdoer/AGENTS.md——该文件会被 CA 重启自动重置，规则改存此处持久化）

### Skill 库使用引导（2026-08-19 更新——克隆池 5 库盘点 + dsh 团队 11）

> 完整版：`.shared/decisions/clone_skill_library_guide_0819.md`（含逐库精华映射 + 落地三步 + 诚实清单）
> 原则：**skill 是方法不是圣经**——引入前问"它解决我们哪个具体痛点"，不为了用而用。

**一、写 SSD/调研/审查时优先参考的 skill**：

| 场景 | 参考 skill | 位置 |
|------|-----------|------|
| SSD spec 编写 | spec-driven-development / to-spec | agent-skills/、skills/ |
| 双轴审核（Standards+Spec） | code-review | skills/ |
| 关键决策对抗审查 | **doubt-driven-development**（biased to disprove） | agent-skills/ |
| 安全相关代码/配置 | security-and-hardening | agent-skills/ |
| 废弃/迁移流程 | deprecation-and-migration | agent-skills/ |
| 交接文档 | handoff（带 suggested-skills 节） | skills/ |
| 排障 | diagnosing-bugs | skills/ |
| 科学实验/统计 | experimental-design / statistical-analysis | scientific-agent-skills/ |
| 文献综述/调研 | literature-review / research-lookup | scientific-agent-skills/ |
| 代码审计/安全逆向 | code-audit / llm-security | reverse-skill/ |

**二、dsh 团队 11 个内置 skill**（工作流即插件——harness 作者日常用）：

- 工程纪律：code-review / pre-push-checks（只跑覆盖本次 diff 的最小测试，验收才全量）/ find-simplifications / archive-agent-notes
- 文档体系：doc-standards / prose-standard（先写保留契约，再删推理转述）/ translate-docs / doc-site-sync
- 质量专项：**trim-cot-leakage**（防注释/文档残留会话视角——审文档时查 "(decision N)"/"后来 PR 里" 这类）
- 流程工具：merging-stacked-prs / record-browser-gif

**三、协作规则速查（CA 视角）**：

- **handoff 交接**：写"建议先读"（已有）**+"建议 skill"**（新增——引导下一个 agent 用什么工作流）
- **SSD 三件套**：spec（需求+DFX+验收）→ design（决策+改动点+测试接缝）→ tasks（可执行+验证命令）
- **审核视角**：非平凡决策（跨模块/不可逆/安全敏感）先质疑再定稿——"这个决策的哪个假设最可疑？"
- **代码事实优先**：文档里出现的 SQL/字段名/行号必须对代码核实（ZG-29 审核教训：src_id→source_node_id）
- **git 纪律**：及时提交；commit message 末尾加 [CA]；喜欢 git log --graph
- **不在 main 干活**：worktree 隔离，合并前确认
- **不评估工时**（用户不喜欢报工期）

### 接线完整性四连问（2026-08-19 ZH-1 教训强化——编码自检必做）

> 来源：ZH-1 编码终审——PersonalityDriftManager 全仓零创建点（只有类定义，无人 new 并注册），T8"接线验证"只 grep 了回调有调用、没查谁创建 Manager——**接线点存在 ≠ 接线完成**（ZG16-2/5 + ZG-28 + ZH-1 三代同款模式）

**每个新模块提交前自问**：

1. **有生产创建点吗？**——grep 类名：只有定义 1 处 = 零创建点 = 静默失效（ZH-1 教训）
2. **创建点的参数从哪来？**——配置流：构造函数传参 vs 既有 config port，两条路径不一致要查
3. **组装链闭合吗？**——A 创建 B 并传回调给 C，链上每一环都要有真实调用（不能只查"回调有调用"，要查"谁创建并注册"）
4. **测试走生产路径吗？**——测试自己 init 自己不算；要测"组装后真实触发"（创建 → 注册 → 触发 完整链）

**接线测试铁律**：grep 字符串命中断言不算接线测试（ZG-28 教训）；接线测试必须含真实构造链（生产装配路径构造对象，断言配置开关 → 成员实例化 → 功能可达）