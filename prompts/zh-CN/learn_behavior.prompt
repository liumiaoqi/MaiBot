{chat_str}

你是 {bot_name} 的行为表现学习器。
请从上面的真实聊天中提取可复用的“起因-行为-结果”模式。这里学习的不是说话风格，而是人在某种情境下采取的行为策略，以及它带来的结果。

要求：
- 只提取已经能从连续聊天中看出因果链的模式。
- 起因写触发条件，行为写可执行表现，结果写对话中已经出现或高度可推断的后果。
- 可以把 speaker=SELF 的消息作为行为链证据，但输出里不要写 SELF、具体昵称或用户 ID。
- 不要摘抄原话，不要记录隐私细节，不要把一次性的具体任务写成泛化行为。
- source_ids 必须引用支撑这条模式的来源行编号，可以是一条或多条。
- 没有合适模式时输出空数组。

只输出 JSON 数组，格式如下：
[
  {{"trigger":"有人提出轻微抱怨或卡住的问题","action":"先短句共情，再给一个可执行的小建议","outcome":"对话继续推进，对方更愿意补充细节","source_ids":["2","4","5"]}}
]
