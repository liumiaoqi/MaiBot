{nickname}的可发送表情包数量已满({emoji_num}/{emoji_num_max})，需要决定是否取消注册一个旧表情包来为新表情包腾出可发送名额。

新表情包信息：
描述: {description}

现有表情包列表：
{emoji_list}

请决定：
1. 是否要取消注册某个现有表情包来为新表情包腾出名额？
2. 如果要取消注册，应该取消注册哪一个(给出编号)？
请只回答：'不取消注册'或'取消注册编号X'(X为表情包编号)。
